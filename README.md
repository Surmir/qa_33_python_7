# qa_33_python_7(vasilii_kasatov)

#В этом проэкте реализованы следующие тесты API сервиса «Яндекс.Самокат»:

# 1.Courier(test_api_courier_create.py)
    # 1.1 проверка создания курьера
    # 1.2 проверка создания курьера с уже существующим логином
    # 1.3 проверка создания курьера, без логина или пароля

# 2.Courier(test_api_courier_login.py)
    # 2.1 проверка авторизации курьера
    # 2.2 проверка авторизации курьера, с неправильным логином или паролем
    # 2.3 проверка авторизации курьера, с пустым логином или паролем

# 3.Orders(test_api_orders.py)
    # 3.1 проверка создания заказа, с разными данными в color
    # 3.2 проверка получения списка заказов
   
# Обнаружены расхождения с документацией:

    # Во всех сообщениях об ошибках в тестах тело ответа содержит дополнительное 
    поле({"code": xxx, "message": ...}), а по документации API должно 
    только({"message": ...})

    # Тело сообщения в тесте 1.2(test_courier_create_same_login_error)
    сообщение об ошибке({"message": "Этот логин уже используется. Попробуйте другой."}) 
    отличается от API документации({"message": "Этот логин уже используется"})
